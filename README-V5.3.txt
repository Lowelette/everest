EVEREST v5.3 — sidebar filters

GitHub Pages patch: replace js/catalog-runtime.js and css/catalog.css.
No database migration required.

Desktop: filters are a left sidebar. The sidebar is generated from CMS data:
- factories
- series/classes
- every filter group and its values
- price from/to
Multiple values can be selected. OR inside one group, AND between groups.
Mobile: filters collapse under a "Показать фильтры" button.
